node_modules/
*.log
.DS_Store

# Build output — regenerated by `npm run build:tokens` and the react build
# (the publish workflow rebuilds these in CI before `npm publish`).
dist/
react/dist/
*.tsbuildinfo
